#initializing the array
arr = [9,6,7,5,20]

#making  k as the lower bound
k=0

while k < len(arr):
      # Visit each element in the array
      print(arr[k])
      #Increase the counter
      k = k+1
      

# Print the total number of elements in the array
print("Total number of elements in the array:", 5)      